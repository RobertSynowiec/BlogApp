import styles from './PostAdd.module.scss';

const PostAdd = () => {
    return (
        <div className={styles.hero}>
            <h1 className={styles.title}>PostAdd</h1>
            <p className={styles.subtitle}>Subtilte</p>
        </div>
    );
};

export default PostAdd;